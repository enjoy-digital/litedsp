# Power meter

`LiteDSPPower` — `litedsp.level.power` — category `level`

latency: variable (data-dependent) · CSR: yes · bypass: no

## Overview

Average power meter: passes the I/Q stream through and measures mean ``I**2 + Q**2``.

The instantaneous power is accumulated over ``2**window_log2`` accepted samples, then the
block average (accumulator >> window_log2) is latched into ``power`` and ``update`` pulses.
Unlike the original tetra ``LiteDSPPower``, the averaging window is actually implemented.

## Parameters

| Parameter | Default | Type | Description |
|---|---|---|---|
| `data_width` | `16` | int | Sample width in bits (signed Qm.n; default Q1.15). |
| `max_window_log2` | `20` | int | Upper bound of the runtime ``window_log2`` setting (window up to 2**max_window_log2 samples). Sizes the accumulator (2*data_width + max_window_log2 bits) and counter. |

## Ports

| Port | Direction | Layout |
|---|---|---|
| `sink` | sink | iq |
| `source` | source | iq |

Streams follow the LiteX `valid`/`ready` contract (see `doc/interfaces.md`).

## Register Map

### `window` (read-write, 5 bits)

Averaging window as a power of two (window = 2**window_log2).

### `power` (read-only, 32 bits)

Latest block-averaged power (I**2 + Q**2).

## FPGA Resources

| Device | LUT | FF | BRAM | DSP | Fmax (MHz) |
|---|---|---|---|---|---|
| ecp5 | 969 | 106 | 0 | 2 | 143.4 |
| xilinx | 189 | 106 | 0 | 2 | — |

Measured by the `impl/` flows at the registry configuration; regenerated by `python3 impl/report.py` (budget-gated in CI).

## Verification

Golden-model tests: `test/test_power.py` (bit-exact/SNR under randomized backpressure).
